"""Project settings and constants.

Keep framework-agnostic values here (screen dimensions, fps, etc.) so they are
shared by PyGame now and can be reused by other engines later.
"""

DEFAULT_WIDTH = 1280
DEFAULT_HEIGHT = 720
DEFAULT_FPS = 60
